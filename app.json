{
  "name": "slack-choose-command",
  "description": "add `choose` command to your workspace",
  "repository": "https://github.com/ahuglajbclajep/slack-choose-command",
  "env": {
    "VERIFICATION_TOKEN": {
      "description": "Verification token",
      "required":  true
    },
    "OAUTH_TOKEN": {
      "description": "OAuth Access Token",
      "required":  true
    }
  }
}
